<h1 class="text-center" style="text-align: center; background-color: #007bff; color: white; padding: 10px;">Events</h1>
<% unless events.empty? %>
  <%= render partial: 'shared/event', collection: events, locals: { caller: caller } %>
<% else %>
  <p class="text-center">No events found</p>
<%end %>